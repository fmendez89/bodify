import styles from "./styles.module.css";

const BlogPage = () => (
    <section className={styles.section}>
        <div className={styles.sectionRoot}>Blog</div>
    </section>
);

export default BlogPage;
